# Swift / SwiftPM build artifacts
.build/
*.xcodeproj
.swiftpm/

# Assembled app bundle (produced by build.sh) and release archives
ClaudeBar.app/
ClaudeBar-*.zip

# macOS
.DS_Store

# Local-only notarization script (not distributed)
notarize.sh
